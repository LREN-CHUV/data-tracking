machine:
  python:
    version: 3.6.0
  services:
    - docker
dependencies:
  override:
    - docker info
test:
  pre:
    - pip install psycopg2
    - pip install airflow
    - pip install pydicom
    - pip install sqlalchemy
    - pip install python-magic
    - pip install nibabel
  override:
    - cd ./tests && ./test.sh
