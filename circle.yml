machine:
  python:
    version: 3.5.2
  services:
    - docker
dependencies:
  pre:
    - pip install alembic
    - pip install psycopg2
    - pip install airflow
    - pip install pydicom
    - pip install sqlalchemy
  override:
    - docker info
test:
  override:
    - cd ./tests && ./test.sh